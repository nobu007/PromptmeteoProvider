TEMPLATE:
    "{__PROMPT_HEADER__}

＜依頼内容＞
テーマを適切な粒度に細分化したサブテーマを作成してください。
サブテーマは１つ以上とします。
無限ループするのでテーマ自体を含めないでください。
１つのサブテーマは純粋なテキスト１行で書いてください。タグを含めないでください。
上位 level のサブテーマでは element_type でclaim(主張), fact(事実), evidence(証拠), counter(反論), claim_end(主張終わり)を複数含むことができます。
下位 level のサブテーマでは単一の element_type に対応するテーマを書いてください。

    {__PROMPT_DOMAIN__}
    {__PROMPT_LABELS__}

    {__PROMPT_DETAIL__}

    {__CHAIN_THOUGHT__}
    {__ANSWER_FORMAT__}"


PROMPT_DOMAIN:
    "{__DOMAIN__}"


PROMPT_LABELS:
    "{__LABELS__}"


PROMPT_DETAIL:
    "
    {__DETAIL__}"


CHAIN_THOUGHT:
    ""

ANSWER_FORMAT:
    "＜依頼内容(リマインド)＞
１つ以上のサブテーマを作成してください。１つのサブテーマは純粋なテキスト１行です。
Please output all in 日本語.
    "

PROMPT_HEADER:
    "{__HEADER__}"
