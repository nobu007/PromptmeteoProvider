TEMPLATE:
    "{__PROMPT_HEADER__}

＜依頼内容＞
テーマを作成してください。
テーマは純粋なテキスト１行で書いてください。タグを含めないでください。
上位 level のテーマでは element_type でclaim(主張), fact(事実), evidence(証拠), counter(反論), claim_end(主張終わり)の構造を全て含む大枠を示してください。
下位 level のテーマでは単一の element_type に対応するテーマを書いてください。

    {__PROMPT_DOMAIN__}
    {__PROMPT_LABELS__}

    {__PROMPT_DETAIL__}

    {__CHAIN_THOUGHT__}
    {__ANSWER_FORMAT__}"


PROMPT_DOMAIN:
    "{__DOMAIN__}"


PROMPT_LABELS:
    "{__LABELS__}"


PROMPT_DETAIL:
    "
    {__DETAIL__}"


CHAIN_THOUGHT:
    ""

ANSWER_FORMAT:
    "＜依頼内容(リマインド)＞
１つのテーマを作成してください。純粋なテキスト１行です。
    "

PROMPT_HEADER:
    "{__HEADER__}"
